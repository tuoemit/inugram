From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Sun, 12 Apr 2026 21:03:46 +0300
Subject: replacements for web previews

---
 .../src/main/java/org/telegram/ui/ChatActivity.java        | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -13837,6 +13837,12 @@ public class ChatActivity extends BaseFragment implements
             return;
         }
         checkEditLinkRemoved(charSequence);
+        if (force && foundWebPage != null && !(foundWebPage instanceof TLRPC.TL_webPagePending) && desu.inugram.helpers.WebPreviewHelper.allUrlsPresent(foundUrls, charSequence)) {
+            if (messagePreviewParams != null) {
+                messagePreviewParams.updateLink(currentAccount, foundWebPage, chatActivityEnterView.getFieldText(), replyingMessageObject == threadMessageObject ? null : replyingMessageObject, replyingQuote, editingMessageObject);
+            }
+            return;
+        }
         if (force && foundWebPage != null && !(foundWebPage instanceof TLRPC.TL_webPagePending)) {
             if (foundWebPage.url != null) {
                 int index = TextUtils.indexOf(charSequence, foundWebPage.url);
@@ -13964,6 +13970,7 @@ public class ChatActivity extends BaseFragment implements
             } else {
                 req.message = textToCheck.toString();
             }
+            req.message = desu.inugram.helpers.WebPreviewHelper.applyReplacements(req.message);
             if (foundWebPage != null && req.message.equals(foundWebPage.displayedText)) {
                 return;
             }
